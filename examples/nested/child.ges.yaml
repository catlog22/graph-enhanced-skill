# 子图 — 接收 _input，产出 child_result
schema: ges/1.1
meta:
  name: child-skill
  entry: process
  terminal: [done]
  output: [child_result]

nodes:
  process:
    actions:
      - id: analyze
        prompt: "分析输入: {{_input}}"
        output: [child_result]

edges:
  - { from: process, to: done }
